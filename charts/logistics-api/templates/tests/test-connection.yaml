apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "logistics-api.fullname" . }}-test-connection"
  labels:
    {{- include "logistics-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "logistics-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
